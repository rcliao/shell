# Cycle 16 — 2026-05-07 — substantive ships

**Type:** ship (after retrospective approval)

## Shipped

### B-006 — umbreon reflect rules harmonized ✅
- Backup: `~/.shell/agents/umbreonmini/memory.db.bak-2026-05-07-c16`
- `sys-promote-to-ltm`: access_gt 50→3, age_gt_hours 72→24
- `sys-decay-unaccessed`: age_gt_hours 48→72, access_lt 10→3
- `sys-demote-stale-ltm`: migrated cond_unaccessed_gt_hours → cond_age_gt_hours=168
- `sys-pin-identity`: inserted (was missing)
- Effect: future reflect runs will preserve umbreon's identity tier and promote behavioral memories on the same schedule as pikamini.

### B-012 — access_count inflation bug ✅
- Root cause: `applyMerge` and `applyDedup` in `ghost/internal/store/reflect_merge.go` were SUMming access_count/utility_count across merged groups and writing the total back to the survivor. Repeated merges over weeks inflated counts pathologically (observed: 10.6 trillion).
- Fix: changed SUM → MAX in both functions. Branch `evolve/c16-access-count-max`, commit `89d891b`. `go vet` clean, reflect tests pass.
- Data migration: clamped `access_count = MIN(access_count, 10000)` on both `~/.ghost/memory.db` (15 rows) and `~/.shell/agents/umbreonmini/memory.db` (16 rows). Backups saved alongside.
- Branch is local-only — push when ready for review.

### Loop meta-improvements (from retrospective)
- README updated with auto-stop heuristic, two-tier cadence, and per-cycle cost-tracking placeholder.
- Rotation list refreshed: agent self-reflections (B-018) replaces transcript topic clustering as a default angle.

## Not shipped this cycle
- **B-017** (heartbeat prompt tension) — still blocked by 54 modified bridge files in shell repo. Will ship after WIP commits.
- **B-016** (storage decision-tree convention) — would be a ghost put for both agents; can ship next cycle.
- **B-013, B-014, B-007 install-with-restart** — pending shell repo cleanup.

## State updates
- last_cycle: 15 → 16
- shipped_count: 3 → 5
- New ghost branch: `evolve/c16-access-count-max` (one commit, local-only)

## Next cycle
- Approvals first (B-016 is cheap; B-017 unblocks once shell WIP commits).
- Push `evolve/c16-access-count-max` to GitHub if user wants a PR.
- Re-measure umbreon's heartbeat noop rate in 24h to validate B-006 effect.
