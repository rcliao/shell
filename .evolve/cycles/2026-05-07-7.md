# Cycle 7 — 2026-05-07 — heartbeat prompt diff (B-011 prep)

**Type:** observation (no approvals)
**Rotation angle:** heartbeat noop investigation — diff the actual prompts

## Approvals scanned
0 approved. All proposed/observing/shipped status unchanged.

## Transcript delta
- shared: 1963 → 1972 (+9)
- umbreon local: 2045 → 2052 (+7)

## Heartbeat prompt diff: pikamini vs umbreon

**Result: IDENTICAL.** Both agents receive byte-for-byte the same heartbeat prompt (the assembly in `internal/bridge/heartbeat.go` is agent-agnostic).

### Both configs have:
- `system_prompt: ""` — empty
- Same scheduler/heartbeat/memory blocks
- Same prompt template lines 100–209 of heartbeat.go

### What does differ (`agent.skills` field):
| Field | Pikamini | Umbreon |
|---|---|---|
| skills (declared) | coding, research, web-search, image-generation, planning, tool-use | emotional-support, verification, code-review, daily-planning, companionship |

These are **declared/advertised skills** for delegation routing — not actual SKILL.md loaded. They surface in cross-agent task assignment but don't enter the heartbeat prompt.

## Implication for B-011

The pikamini-56% vs umbreon-29% heartbeat noop gap is **not from prompt asymmetry**. The system prompt is empty; the heartbeat prompt is identical. The ONLY mechanism shaping each agent's voice differently is:

1. **Ghost identity memories** retrieved into context per-namespace
2. **Recent transcript exchanges** (different agents see different conversations)

Since (2) cuts both ways (umbreon's transcripts are *more* companionship-focused, would push toward more responses — confirmed), the durable lever is (1) — and umbreon's ghost identity is currently weaker because:
- `sys-pin-identity` reflect rule missing (per cycle 1 finding)
- Harsher decay (access>50 to reach LTM) means identity memories age out faster
- Smaller corpus (938 vs 5288) — less to retrieve from

→ **B-006 is now the single highest-leverage ghost fix.** It's not just memory hygiene; it's the actual mechanism behind the cost gap and behavior asymmetry.

### Heartbeat prompt has an internal tension worth noting

Line 203 (deep-heartbeat branch): *"After reflection, send a brief check-in message to the most recently active chat."*
Line 206 (always): *"If there is genuinely nothing to do, respond with just: [noop]"*

These conflict. Each agent resolves it differently based on its identity context. The agent that "feels less anchored" defaults to fulfilling the more concrete instruction (send a check-in).

## New backlog
- **B-017** — Resolve heartbeat prompt internal tension. Either gate the "send a check-in" instruction more clearly (e.g., "send a check-in only if you have something specific to share, not as a default") or remove it. Low risk, prompt-only change. Re-measure noop rates after to validate.

## State updates
- last_cycle: 6 → 7
- shared HWM: 1963 → 1972
- umbreon HWM: 2045 → 2052
- rotation_next: ghost-reflect (revisit umbreon's status post-B-006 if/when shipped)

## Recommendations
1. **Approve B-006** — gives the biggest behavior delta for the cheapest SQL.
2. **Approve B-017** — single prompt edit, can re-measure within a day.
3. **Approve B-016** — single ghost put, fixes the dual-storage confusion observed in cycle 6.

These three together would likely shift umbreon's heartbeat noop rate noticeably. We can measure within 2 cycles after they ship.
