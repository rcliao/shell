# Cycle 4 — 2026-05-07 — real noop telemetry

**Type:** observation (no approvals to ship)
**Rotation angle:** skill telemetry done right (per-agent `shell.db` empty-assistant rows)

## Approvals scanned
B-001, B-002, B-003, B-004, B-006, B-008, B-010 — all still `proposed`. No ships.

## Transcript delta
- shared HWM: 1943 (unchanged)
- umbreon local: 2027 → 2034 (+7 msgs)
- 54 WIP files in shell repo (active dev session). Restart still off-limits.

## Real noop rate (cycle 3 fixed the metric)

Query: `COUNT(*) FILTER (WHERE role='assistant' AND TRIM(content)='')` on per-agent `shell.db`.

### Pikamini — overall 42/427 = **9.8%**
| chat_id | noops | total | rate |
|---|---|---|---|
| 0 (heartbeat session) | 40 | 71 | **56.3%** |
| -1003731277835 (family group) | 1 | 243 | 0.4% |
| 1020995031 (papi DM) | 1 | 21 | 4.8% |
| 832881763 (mami DM) | 0 | 92 | 0% |

### Umbreon — overall 26/560 = **4.6%**
| chat_id | noops | total | rate |
|---|---|---|---|
| 0 (heartbeat session) | 25 | 86 | **29.1%** |
| -1003731277835 (family group) | 1 | 250 | 0.4% |
| 832881763 (mami DM) | 0 | 219 | 0% |
| 1020995031 (papi DM) | 0 | 5 | 0% |

## Findings

1. **Heartbeat noop suppression works as designed.** Recent pikamini noops cluster at hourly timestamps (00:01, 03:02, 04:01, 14:01, 17:01, 20:01, 23:01) — exactly heartbeat fires.

2. **Pikamini noops 56% of heartbeats vs umbreon 29%.** Umbreon is ~2× more likely to actually emit something on heartbeat. Three possible explanations:
   - **Companionship voice intentional**: umbreon's role is presence — saying "I'm here 💙" on heartbeat is in-character.
   - **Reflect rule asymmetry (B-006)**: umbreon's missing `sys-pin-identity` and harsher decay leave it with less stable identity context, so it generates more on heartbeat to fill the silence.
   - **Heartbeat prompt is the same for both**: I should diff the actual prompt sent per agent before concluding.

3. **Group chat noop 0.4% on both** — agents are NOT silently deferring when addressed. The earlier hypothesis that umbreon "lacks mutual transcript awareness" → never speaks may be a different mechanism (umbreon writes to its local DB; pikamini writes to shared; they don't see each other's recent messages).

4. **Mami DMs: 0% noop on both.** Agents always respond to mami. Healthy.

## New backlog
- **B-011** — Investigate pikamini vs umbreon heartbeat noop gap (56% vs 29%). Diff the actual prompts sent to each, then check if shipping B-006 (reflect rule fix) shifts umbreon's rate. Cheap experiment if we can capture pre/post.

## Cost note for EV
- Heartbeat is firing roughly hourly per agent. Pikamini's 56% noop = ~half its heartbeat tokens are saved. Umbreon's 29% = much more spend on heartbeat content that may not be useful. **B-006 has a plausible cost-reduction angle**, not just a memory-quality one.

## State updates
- last_cycle: 3 → 4
- shared HWM: 1943 (unchanged)
- umbreon HWM: 2027 → 2034
- shipped_count: 3 (unchanged)
- rotation_next: ghost-reflect-cluster-quality (cycle 5, if no approvals)

## Recommendations heading into cycle 5
1. Approve **B-006** (umbreon reflect rules) — cheapest concrete win, plausible cost reduction.
2. Approve **B-001** (telemetry instrumentation) — codifies what cycle 4 just did manually.
3. Consider approving **B-008** (delete legacy `[relay to=...]` parser) — trivial dead-code cleanup, can be batched with any future bridge commit.
