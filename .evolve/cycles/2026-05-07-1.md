# Cycle 1 — 2026-05-07 — observation (ghost reflect rules diff)

**Type:** observation (no approvals to ship)
**Rotation angle:** ghost reflect_rules diff (preps B-006)

## Approvals scanned
All 9 BACKLOG items still `proposed`. No ships this cycle.

## Transcript delta since cycle 0
- shared: +1 msg (id 1943, 09:01 PDT, single line — quiet hour)
- umbreon local: +0 msgs

## Reflect-rules diff (pikamini global vs umbreon local DB)

The 7× ltm/dormant imbalance has a concrete cause: umbreon's rules are bootstrapped with stricter defaults than pikamini's, likely because ghost shipped tighter defaults between 2026-03-08 (pikamini bootstrap) and 2026-03-23 (umbreon bootstrap).

| Rule | Pikamini | Umbreon | Effect on umbreon |
|---|---|---|---|
| `sys-promote-to-ltm` | age>24h, access>3 | age>72h, **access>50** | Almost nothing reaches LTM. 50 accesses is huge. |
| `sys-decay-unaccessed` | age>72h, access<3 | **age>48h**, access<10 | Decay starts sooner, hits more memories. |
| `sys-demote-stale-ltm` | age>168h, access<2 | unaccessed>168h (field migration) | Same intent, different schema field used. |
| `sys-promote-sensory` | (no access threshold) | access>1 | Sensory→STM gated by an access. |

Pikamini also has rules umbreon lacks (created later in the global DB):
- `sys-pin-identity` (2026-03-08, priority 1) — protects `identity` tier from decay/demotion
- All other rules are the same DDL on both sides; only thresholds differ.

## Implication
Umbreon's "personality drift" risk is higher than pikamini's purely from rule asymmetry — important social-companion memories age out before they can be promoted. Worth fixing before doing any heartbeat tuning.

## B-006 refined
**Proposed harmonization** (umbreon → match pikamini's thresholds):
- `sys-promote-to-ltm`: lower `access_gt` from 50 → 3, `age_gt_hours` from 72 → 24
- `sys-decay-unaccessed`: raise `age_gt_hours` from 48 → 72, lower `access_lt` from 10 → 3
- Backfill `sys-pin-identity` rule into umbreon's DB
- Migrate `sys-demote-stale-ltm` to use `cond_age_gt_hours` field consistently

**Risk:** changes future reflect behavior; existing dormant memories stay dormant unless explicitly promoted. Low blast radius (ghost reflect is the only consumer).

**Ship plan if approved:** SQL migration on umbreon's local memory.db, no code changes. Backup DB first. Single ghost reflect run after to verify.

## Other observations this pass
- No new umbreon activity in 1.5h — agent is quiet during papi's camping trip.
- `reflect_rules` schema has a `created_by` column defaulting to `system` — agents can in principle author their own rules. Future hook for behavior evolution: per-agent custom rules in `agent:<n>` scope.

## Next cycle (cycle 2) — proposed
- If anything approved by then: ship.
- Otherwise rotate angle to: **transcript topic clustering** for pikamini's last 200 msgs to surface recurring patterns that could become skill drafts (B-007 prep).

## State updates
- shared HWM: 1942 → 1943
- umbreon HWM: 2027 (unchanged)
- shell sha: d2c27b3 (unchanged)
- ghost sha: 3920d8f (unchanged)
