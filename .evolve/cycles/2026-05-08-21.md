# Cycle 21 — 2026-05-08 — loop redesign + autonomous ships

**Type:** redesign + ship (loop resumed from halt)
**Trigger:** user approved retrospective recommendations + autonomous shipping for low-risk

## Loop redesign shipped (B-018 expanded)

### Two-tier approval (README §Design rules updated)
Low-risk (auto-ship): ghost puts (single-agent ns, non-identity), per-agent skill drafts, prompt-only edits with tests, README/docs, dead-code removal w/ 0 invocations in 30+d, additive reflect rules.
High-risk (gated): DB schema changes, data migrations, mass updates, deletions of identity memories, force pushes, secrets, breaking API changes, restart of running daemons.

### Reflect & consolidate (README §Design rules added)
Each cycle scans for duplicates/supersessions/stale items before adding new findings. Backlog should shrink as often as it grows.

### Agent self-reflections as primary input (B-018)
Was: scan transcripts → derive patterns. Now: read `agent:<n>` memories tagged `behavioral|learning|convention` or kind `procedural` since last cycle, treat each as a candidate finding. Transcript scanning becomes secondary fallback.

### Validating status (README §Status flow updated)
New flow: `proposed` → `validating` (with `predicted-effect:` + `measure-by:` lines) → `shipped`/`regressed`. Items past their measurement deadline get graded; missed predictions trigger auto-revert proposals.

## Backlog reflect & consolidate

**Closed via supersession:**
- B-002 (ledger.jsonl) → already covered by `cycles/*.md` + `state.json`
- B-010 (real noop counter) → cycle-4 method (empty-row query) is sufficient; no instrumentation needed
- B-011 (noop gap investigation) → addressed by B-006 (shipped) + B-017 (pending shell WIP)

**Marked deferred (low-risk but blocked):**
- B-001, B-008, B-017 — all blocked by 54 modified bridge files in shell repo

**New high-risk items (gated):**
- B-019 (added below) — mass-tag existing 900+ auto-summary memories with `auto-generated` so the new B-013 rule can fire on them

## Autonomous ships this cycle

### B-016 — Storage decision-tree convention ✅
- pikamini ghost id `01KR3WS0X8Y9WW7Z62CG0M0DKM` (`agent:pikamini`, ltm, kind `procedural`, tags `learning,convention,evolve-cycle-21`)
- umbreon ghost id `01KR3WS3WHGMB2518EDARRTTS5` (`agent:umbreonmini`, same)
- **predicted-effect:** observable in next storage-related conversation; agents reference the convention.
- **measure-by:** opportunistic.

### B-013a — Auto-summary archive reflect rule ✅
- Inserted `sys-archive-auto-summary` into both DBs (`~/.ghost/memory.db` for pikamini, `agents/umbreonmini/memory.db` for umbreon).
- Conditions: `cond_tag_includes='auto-generated'` AND `cond_tier='dormant'` AND `cond_age_gt_hours=720` → action=ARCHIVE.
- **Status:** infrastructure live but **fires on zero rows today** because no memories carry the `auto-generated` tag yet. Mass-tagging the historical 900+ rows is a high-risk DB cleanup → split out as B-019 (gated).
- **predicted-effect:** zero today; depends on B-019 approval or a future ghost code change to tag auto-summaries at write time.

### B-018 — Loop redesign ✅
- README + BACKLOG rewritten. Documented in this cycle report.

### Note: deep-reflect IS already on for umbreon
User asked to enable it; investigation found it's already firing.
- `defaultDeepReflectInterval = 6` (every 6th heartbeat → deep) per `internal/scheduler/scheduler.go:55`
- pikamini: 12 deep heartbeats fired
- umbreon: 13 deep heartbeats fired
- umbreon writes 35 `behavioral`-tagged memories; pikamini writes 287 `procedural`-kind memories with topic tags
- Both work — stylistic asymmetry, not a missing feature

## New backlog item

### B-019 — Mass-tag existing auto-summary/exchange-summary memories  *(HIGH-RISK → needs approval)*
- **kind:** mass UPDATE adding `auto-generated` tag (~900 rows: 11+336 pikamini + 562 umbreon)
- **why:** B-013a rule infrastructure exists but fires on 0 rows. Tagging existing rows lets the rule actually archive the dormant clutter that has accumulated.
- **scope:** UPDATE memories SET tags = json_insert(coalesce(tags,'[]'), '$[#]', 'auto-generated') WHERE (key LIKE 'auto-summary-%' OR key LIKE 'exchange-summary-%') AND tags NOT LIKE '%auto-generated%'.
- **risk:** mass mutation; additive only (just adding tags); reversible. Backups before.
- **predicted-effect:** dormant count drops by ≥30% on each agent within 1 reflect cycle after tagging.

## State updates
- last_cycle: 20 → 21
- halted: false (resumed)
- shipped_count: 5 → 8 (B-016, B-013a, B-018)
- backlog: 14 active → 7 active + 6 shipped + 3 superseded + 1 observing

## Next cycle
- Approvals first (B-019, B-014, B-003 are the gated ones).
- Primary input now agent self-reflections — read `agent:<n>` memories tagged `behavioral|learning|convention` since cycle 21 timestamp.
- Validation: B-016 will validate opportunistically; B-006 still on its 24h measurement deadline (re-measure umbreon heartbeat noop rate vs cycle-8 baseline 27%).
