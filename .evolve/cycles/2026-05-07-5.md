# Cycle 5 — 2026-05-07 — ghost reflect cluster quality

**Type:** observation (no approvals to ship)
**Rotation angle:** ghost reflect cluster quality + access-count integrity

## Approvals scanned
B-001, B-002, B-003, B-004, B-006, B-008, B-010, B-011 — all still `proposed`.

## Transcript delta
- shared HWM: 1943 → 1960 (+17)
- umbreon local: 2034 → 2035 (+1)

## Cluster health (one-line summary)

| Signal | Pikamini | Umbreon |
|---|---|---|
| Total memories | 5288 | 938 |
| Noisy (access>5, importance<0.4) | 971 (18%) | 161 (17%) |
| High-value (access>10, importance>0.7) | 57 | 53 |
| Never-accessed LTM | 2 | 0 |
| Edges | 17,368 | 5,267 |
| Avg edge weight | 0.22 | 0.59 |

Both agents have similar noise ratios (~17–18%). Pikamini's edge graph is much weaker (avg 0.22 vs 0.59) — likely from auto-similarity edges accumulating without pruning. Umbreon's smaller, denser edge graph is healthier.

## 🚨 Ghost bug: access_count is inflating pathologically

**Umbreon, top-accessed memory:** `auto-summary-1775516489492028000` — access_count = **10,665,021,006,695** (10.6 trillion). The key suffix `1775516489492028000` is a nanosecond timestamp from March 2026.

**Pikamini, top-5 dormant:**
| key | access_count | last_accessed |
|---|---|---|
| `exchange-summary-1773784872969` | 1,085,809 | 2026-05-07 14:21 (today!) |
| `ghost-context-assembly-3-phases` | 271,491 | 2026-03-15 |
| `ghost-context-3phase-pipeline` | 33,459 | 2026-04-04 |
| `mcp-binary-path-gobin` | 24,517 | 2026-03-23 |
| `chat-id-mapping-confirmed` | 18,933 | 2026-04-02 |

**Hypothesis:** somewhere in the access path, `access_count` is being incremented by a non-1 value (timestamp? chunk id? raw count from FTS5?). The umbreon entry literally has its timestamp magnitude as the count. Pikamini's `exchange-summary-1773784872969` was last_accessed today and has an access_count >1M — clearly impossible for a 1.5-month-old memory.

**Side effect:** `sys-prune-low-utility` rule (access>20, utility<5%) catches 9+ pikamini memories. But they're already dormant, so the rule is a no-op for them. The corpus has accumulated a layer of "stuck dormant with inflated counts" memories that no rule reaches.

## Recommendations

### B-012 (new) — Fix access_count inflation in ghost
- **status:** proposed
- **kind:** code-fix (ghost repo)
- **why:** access_count values in the millions/trillions, with key-as-timestamp pattern strongly suggesting timestamp-as-increment. Corrupts prune-low-utility, scoring, and any cost projection. Real bug, surfaced by today's measurement.
- **scope:** find the increment site in ghost (likely `internal/store/` UPDATE on access_count). Verify it's `access_count + 1` not `access_count + <some_value>`. Add a one-shot migration to clamp `access_count = MIN(access_count, 99999)` or recompute from chunk access logs if available. Backup DBs first.
- **risk:** low. Read-side scoring will improve once true counts are restored; current state is corrupt anyway.

### B-013 (new) — Prune `auto-summary-*` / `exchange-summary-*` rule
- **status:** proposed
- **kind:** ghost reflect rule
- **why:** Both agents accumulate auto-generated exchange summaries that linger in dormant. They're auto-generated, low-utility (mostly utility_count=0), and clutter top-access lists.
- **scope:** new reflect rule: `tag_includes='auto-summary'` OR key glob `auto-summary-*`/`exchange-summary-*` AND tier=dormant AND age>30d → DELETE. Or ARCHIVE first, then DELETE after another 30d.

### B-014 (new) — Edge weight pruning for pikamini
- **status:** proposed
- **kind:** ghost reflect / housekeeping
- **why:** pikamini avg edge weight 0.22 vs umbreon 0.59. Many weak edges → noisy expansion → bad context retrieval per the existing `ghost-improve` skill's diagnosis pattern. Concrete action: delete edges where weight < 0.15 OR run with `GHOST_EDGE_MIN_WEIGHT=0.3` to test.
- **scope:** measure first (eval CLI), then prune.

## Back to the original mandate
None of these are urgent for behavior evolution, but they're important for ghost health, which underwrites the personality/identity backbone. Fixing access_count (B-012) is high-leverage — every other ghost decision (consolidation, decay, retrieval scoring) is currently noisy because of corrupt counts.

## State updates
- last_cycle: 4 → 5
- shared HWM: 1943 → 1960
- umbreon HWM: 2034 → 2035
- rotation_next: skill-telemetry-real (back to top of rotation)

## Next cycle
- Approvals first.
- Strong recommendation: approve **B-012** + **B-006** together. Both are ghost-side fixes, both improve the substrate behavior evolution depends on.
